spring:
  application:
    name: d_serv

server:
  port: 8765

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8765/eureka/